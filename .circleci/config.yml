version: 2.0

jobs:
  lint:
    docker:
      - image: circleci/openjdk:8-jdk-browsers
    steps:
      - checkout
      - run: ls -l
      - run: ./gradlew test
    store_test_results:
      test-results: build/test-results/test
  test:
    docker:
      - image: circleci/openjdk:8-jdk-browsers
    steps:
      - checkout
      - run: ./gradlew test
    store_test_results:
      test-results: build/test-results/test
  build:
    docker:
      - image: circleci/openjdk:8-jdk-browsers
    steps:
      - checkout
      - run: ./gradlew jar
      - store_artifacts:
          path: build/libs
          destination: libs

workflows:
  version: 2
  main:
    jobs:
      - lint
      - test:
          requires:
            - lint
      - build:
          type: approval
          requires:
            - test